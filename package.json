{
  "name": "tangentfeed-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "site"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build -w packages/core && npm run build -w packages/adapter-idb && npm run build -w packages/adapter-sqlite && npm run build -w packages/crypto && npm run build -w packages/transport-broadcast && npm run build -w packages/transport-webrtc && npm run build -w packages/signaling-server && npm run build -w packages/schema && npm run build -w packages/tangentfeed && npm run build -w packages/react",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "site": "npm run build -w site"
  },
  "devDependencies": {
    "tsup": "^8.5.1"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.2"
  }
}