name: tangentfeed
description: >-
  Offline-first, peer-to-peer data sync. Dart implementation of the tangentfeed
  protocol, validated by the shared conformance vectors.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
